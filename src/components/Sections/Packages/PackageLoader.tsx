import { type FC } from "react";

const PackageLoader: FC = () => {
  return (
    <div className="my-14 text-center">
      <h2 className="text-4xl font-bold">Loading...</h2>
    </div>
  );
};

export default PackageLoader;
